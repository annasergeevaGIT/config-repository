external:
  # use env variables for logins pw and urls
  discovery-service-url: ${DISCOVERY_SERVICE_URL}
  # increase timeout
  default-timeout: 5s

management:
  endpoints:
    web:
      exposure:
        include: refresh